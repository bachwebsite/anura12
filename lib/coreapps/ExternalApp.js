"use strict";class ExternalApp extends App{manifest;source;constructor(manifest,source){super();this.manifest=manifest;this.name=manifest.name;this.icon=source+"/"+manifest.icon;this.source=source;this.package=manifest.package;}
async open(){if(this.manifest.type==="auto"){const win=anura.wm.create(this,this.manifest.wininfo);const iframe=document.createElement("iframe");const bg=this.manifest.background||"#202124";iframe.setAttribute("style","top:0; left:0; bottom:0; right:0; width:100%; height:100%; "+
`border: none; margin: 0; padding: 0; background-color: ${bg};`);console.log(this.source);iframe.setAttribute("src",`${this.source}/${this.manifest.index}`);win.content.appendChild(iframe);iframe.contentWindow.anura=anura;iframe.contentWindow.AliceWM=AliceWM;return win;}
else{const req=await fetch(`${this.source}/${this.manifest.handler}`);const data=await req.text();top.window.eval(data);loadingScript(this.source,this);taskbar.updateTaskbar();alttab.update();return;}}}